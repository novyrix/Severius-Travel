import { bookingPolicyMetadata } from '@/lib/metadata'

export const metadata = bookingPolicyMetadata

export default function BookingPolicyLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
